pipeline{

    agent any

    stages{

        stage('Build Docker Image'){
            steps{
                sh 'echo "executando comando docker build"'
            }
        }
           stage('Push Docker Image'){
            steps{
                sh 'echo "pushando comando docker build"'
            }
        }
    }    
}
